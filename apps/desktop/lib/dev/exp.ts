import { db } from "@kksh/api/commands"
import * as path from "@tauri-apps/api/path"
import { parse, safeParse } from "valibot"

export async function useTestDB() {
	// const appDataDirPath = await path.appDataDir()
	// const p = await path.resolve("/home/kevin/.local/share/kunkun/../../../.ssh/authorized_keys")
	// console.log("p", p);
	// const extDB = new db.JarvisExtDB(1)
	// const searchRes = await extDB.search({
	//   // fullTextSearch: false,
	//   // searchText: "fullTextSearch",
	//   fields: ["search_text"]
	// })
	// console.log("searchRes", searchRes)
}
