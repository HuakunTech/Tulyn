// import type { Remote } from "comlink"
// import { type IOpen } from "../client"

// export function constructOpenAPI(api: Remote<IOpen>): IOpen {
// 	return {
// 		url: (url: string) => api.url(url),
// 		file: (path: string) => api.file(path),
// 		folder: (path: string) => api.folder(path)
// 	}
// }
