query AllExtensions {
  extensionsCollection {
    edges {
      node {
        identifier
        version
        api_version
        name
        downloads
        short_description
        long_description
        icon
      }
    }
  }
}